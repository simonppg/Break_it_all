set noparent

linelength=80

filter=-whitespace/indent # We don't want this warning 'private: should be indented +1 space inside class', also conflict with .editorconfig
filter=-whitespace/comments # Conflict with clang-format
filter=-build/c++11 # All use of chrono library
filter=-build/include_subdir # We don't want this warning 'Include the directory when naming header files'
filter=-legal/copyright # We don't want this warning
